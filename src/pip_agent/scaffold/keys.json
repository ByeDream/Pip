{
  "_comment": "Optional additional API keys for rotation. The primary key still comes from .env::ANTHROPIC_API_KEY (loaded as profile 'env'). Profiles here are ADDITIVE — leave api_key empty to disable an entry. Fill api_key to enable rotation. base_url may be omitted to inherit .env::ANTHROPIC_BASE_URL.",
  "profiles": [
    {
      "name": "backup",
      "api_key": "",
      "base_url": ""
    }
  ]
}
